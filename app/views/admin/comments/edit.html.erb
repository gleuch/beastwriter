<% form_for(@comment, :url => admin_comment_path, :method => :put) do |f| %>
  <fieldset>
    <legend>Edit Link</legend>
    <%= render :partial => 'form', :locals => {:f => f} %>
  </fieldset>
<% end %>